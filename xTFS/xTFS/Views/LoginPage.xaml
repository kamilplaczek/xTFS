<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" BindingContext="{Binding Source={StaticResource Locator}, Path=Login}"
             x:Class="xTFS.Views.LoginPage">
  <Grid VerticalOptions="Center" Margin="20">
	<Grid.RowDefinitions>
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	</Grid.RowDefinitions>
	<Label Text="Login" Grid.Row="0"></Label>
	<Entry Text="{Binding Login}" Grid.Row="1"></Entry>
	<Label Text="Password" Grid.Row="2"></Label>
	<Entry Text="{Binding Password}" Grid.Row="3"></Entry>
	<Button Text="Login" Command="{Binding LoginCommand}" Grid.Row="4"></Button>
	<Button Text="Settings" Command="{Binding SettingsCommand}"  Grid.Row="5"></Button>
  </Grid>
</ContentPage>